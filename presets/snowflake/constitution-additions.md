<!-- Placeholder appended to base constitution by `dbt-specify init --warehouse snowflake`.
     Full Snowflake-specific articles land in Phase C (T-15). -->

## Article S1 — Clustering keys are explicit decisions (placeholder)

Tables larger than 1 GB declare a clustering key in `config(cluster_by=[...])`.
Full Snowflake clustering, warehouse-sizing, query-tag, masking, dynamic-table, and
Cortex articles are filled in during Phase C.
