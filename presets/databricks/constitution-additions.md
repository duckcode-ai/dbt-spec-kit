<!-- Placeholder appended to base constitution by `dbt-specify init --warehouse databricks`.
     Full Databricks-specific articles land in Phase C (T-18). -->

## Article D1 — Liquid Clustering, not partitioning (placeholder)

New tables use Liquid Clustering (`CLUSTER BY (col)`), not rigid partition columns.
Full Databricks Liquid Clustering, Photon, Unity Catalog, materialized-view, query-tag,
and AI/ML articles are filled in during Phase C.
